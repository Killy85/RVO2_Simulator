using RVO;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class DisplayDetails : MonoBehaviour {

/*
    internal RVOSimulator sim_;
    internal int id;
    GameObject panel;
    // Use this for initialization

    private void OnMouseOver()
    {
        Debug.LogError("Naaaan");
        GameObject.Find("id").GetComponent<Text>().text = "Identifiant : " + id;
        GameObject.Find("vel").GetComponent<Text>().text = "Velocity : x ->" + sim_.getAgentVelocity(id).x_ + ", y -> " + sim_.getAgentVelocity(id).y_;
        GameObject.Find("acc").GetComponent<Text>().text = "Acceleration : x ->" + sim_.getAgentAcceleration(id).x_ + ", y -> " + sim_.getAgentAcceleration(id).y_;
        GameObject.Find("pos").GetComponent<Text>().text = "Position : x ->" + sim_.getAgentPosition(id).x_ + ", y -> " + sim_.getAgentPosition(id).y_;
    }

    */

}
